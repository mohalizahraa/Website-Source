{
  "$schema": "https://railway.com/railway.schema.json",
  "//": "Config for the BACKEND service (Root Directory = repo root '/'). Railway builds server/Dockerfile with the repo root as context, so pipeline/ is included. The FRONTEND is a SEPARATE Railway service with Root Directory = 'web' (it uses web/Dockerfile). See DEPLOY_RAILWAY.md.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "server/Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
